#服务端
server:
    port: 8086

spring:
    redis:
        timeout: 6000
        sentinel:
            master: mymaster
            nodes: 127.0.0.1:26379
    application:
        name: user-work
    servlet:
        multipart:
            max-request-size: 100MB
            max-file-size: 50MB

csa:
    filepath: E:/base/boke/  #上传图片位置
    redis: true